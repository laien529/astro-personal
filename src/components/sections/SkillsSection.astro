---
import Container from '@/components/ui/Container.astro';
import SectionHeader from '@/components/ui/SectionHeader.astro';
import SkillCard from '@/components/cards/SkillCard.astro';
import type { SkillsSectionConfig } from '@/data/pageContent';

interface Props {
  config: SkillsSectionConfig;
}

const { config } = Astro.props;
const sectionClass = ['section', config.spacing === 'tight' && 'section-tight'].filter(Boolean).join(' ');
---

<section class={sectionClass}>
  <Container>
    <SectionHeader
      eyebrow={config.header.eyebrow}
      title={config.header.title}
      description={config.header.description}
      align={config.header.align}
    />
    <div class="skills-grid">
      {config.items.map((skill) => <SkillCard label={skill} />)}
    </div>
  </Container>
</section>
